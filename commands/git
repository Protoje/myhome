# Commands for git

# Git pull latest changes:
git pull origin master

# Git add a file 
git add . / <filename>

# Git commit added files
git commit -m "<mesg>"

# Git push commit to remote server
git push

# Git take input to latest commit
git commit --amend
git push --force

# Git merge Branch with another
git checkout master
git merge dlh/<branch>

# Git delete local Branach
git branch -d dlh/<branch>

# Git delete remote Branch
git push origin :dlh/<branch>

# Git rename local branch
git branch -m dlh/<oldbranch> dlh/<neubranch>

# Git rebase with master
git checkout dlh/<branch>
git rebase master
